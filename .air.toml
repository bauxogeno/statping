# Config file for [Air](https://github.com/cosmtrek/air) in TOML format

# Working directory
# . or absolute path, please note that the directories following must be under root.
root = "."
tmp_dir = "tmp"

[build]
# Just plain old shell command. You could use `make` as well.
cmd = "make frontend-build embed live-build"
# Binary file yields from `cmd`.
bin = "tmp"
# Customize binary.
full_bin = "./tmp/statping"
# Watch these filename extensions.
include_ext = ["go", "tpl", "tmpl", "html","vue"]
# Ignore these filename extensions or directories.
exclude_dir = ["tmp","frontend/node_modules", "frontend/dist","logs", "source/dist","dev" ]
# Watch these directories if you specified.
include_dir = []
# Exclude files.
exclude_file = ["statping.db","source/rice-box.go"]
# Exclude unchanged files.
exclude_unchanged = true
# This log file places in your tmp_dir.
log = "air.log"
# It's not necessary to trigger build each time file changes if it's too frequent.
delay = 60000 # ms
# Stop running old binary when build errors occur.
stop_on_error = true
# Send Interrupt signal before killing process (windows does not support this feature)
send_interrupt = true
# Delay after sending Interrupt signal
kill_delay = 5000 # ms

[log]
# Show log time
time = true

[color]
# Customize each part's color. If no color found, use the raw app log.
main = "magenta"
watcher = "cyan"
build = "yellow"
runner = "green"

[misc]
# Delete tmp directory on exit
clean_on_exit = false
